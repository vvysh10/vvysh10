#Write a program to check given nnumber is prime or not
num=int(input("Enter any number"))
for i in range(2,num):
    if num%i==0:
       print(num,"is not prime")
       break
else:
    print(num,"is prime")
    
    
  #algorithm steps:
  1.read value at runtime
  2.read values using for loop in range
  3.check whether num is divisible by range number:
  4.if dividible then it is not a prime number
  break loop using break statement 
  5.else,it is prime number
